%h2 Search Reports

= render 'search_form'

= render partial: 'shared/reports_listing', locals: { reports: @reports }

- unless setting.show_max_reports
  = will_paginate @reports, renderer: FoundationPagination::Rails
