<%= simple_form_for(@report_type) do |f| %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :content, as: :ckeditor, :input_html => { :ckeditor => { :height => 500 } } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
