<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Balrampur Diagnotic Center" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "plugins/jsapi", "chartkick" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render 'shared/navigation' %>
    <%= render 'shared/side_bar' %>
    <div class="large-9 medium-8 columns">
      <%= render 'shared/notice' if flash[:notice] %>
      <%= render 'shared/alert' if flash[:alert] %>
      <div class="row"><%= yield %></div>
    </div>
    <%= javascript_include_tag "application" %>
  </body>
</html>
