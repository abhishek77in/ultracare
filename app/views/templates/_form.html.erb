<%= simple_form_for(@template) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :ultrasound_type %>
    <%= f.input :content, as: :ckeditor %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
